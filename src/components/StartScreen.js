import "./StartScreen.css";

import React from "react";

const StartScreen = () => {
  return (
    <div className="start">
      <h1>Secret Word</h1>
      <button>Start</button>
    </div>
  );
};

export default StartScreen;
